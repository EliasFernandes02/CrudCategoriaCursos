<section class="card card-body container-fluid">
    <h1 class="">Gerenciar Categoria</h1>
    <div class="mt-3 d-flex justify-content-start ">
        <a href="/categorias/novo" class="btn btn-outline-primary me-1">Adicionar categorias</a>
    </div>
    <div>
        <table class="table table-striped table-hover  text-center mt-4">
            <thead class="table-dark">
                <tr>
                    <th>#ID</th>
                    <th>Nome</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                <?php
                foreach ($categorias ?? [] as $cadaCategoria)
                {
                    echo "
                        <tr>
                            <td class='fw-bold'>{$cadaCategoria->id}</td>
                            <td>{$cadaCategoria->nome}</td>
                            <td>
                                <a href='/categorias/editar?id={$cadaCategoria->id}' class='btn btn-warning'>Editar</a>
                                <a href='/categorias/excluir?id={$cadaCategoria->id}' class='btn btn-danger'>Excluir</a>
                            </td>
                        </tr>
                    ";
                }
                ?>
            </tbody>
        </table>
    </div>
    </div>
</section>